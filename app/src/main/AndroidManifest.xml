<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.napp.napp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NApp"
        android:usesCleartextTraffic="true">  <!-- TODO Cleartext traffic is enabled to allow for easier development, but should be disabled when deployed to production -->
        <activity
            android:name=".ui.bookingpage.bookingpage"
            android:exported="true" />
        <activity
            android:name=".ui.welcomepage.welcomepage"
            android:exported="true" />
        <activity
            android:name=".ui.faq.FAQPage"
            android:exported="true" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="true" />
    </application>

</manifest>